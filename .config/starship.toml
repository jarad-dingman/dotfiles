# Minimal module set — only what's actually used, instead of starship's
# default "$all" (every language/cloud/tool detector live simultaneously).
# Each module below keeps starship's own default styling; this file only
# controls which modules are evaluated at all.
format = """
$directory\
$git_branch\
$git_status\
$cmd_duration\
$line_break\
$character"""
