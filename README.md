# Dotfiles

These dotfiles are meant for a macbook since it attempts to use
`brew` and installs multiple mac only utils.

However, it could be fitted to other systems.

### Installation:

`bash
chmod +x install.sh
./install.sh
`
